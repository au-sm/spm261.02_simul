// No Apps Script Web App has been deployed for SPM261.02 yet. Once the
// instructor deploys backend.gs to a new Google Sheet (Extensions >
// Apps Script > paste backend.gs > Deploy > New deployment > Web app,
// Execute as: Me, Who has access: Anyone -- see backend.gs's own header
// comment for the full steps), paste the resulting /exec URL below and
// push that one-line change. Until then the site is fully browsable/
// readable but the Take Base / Negotiate / Weekly Lineup submission
// forms will not actually save anywhere.
const BACKEND_URL = "";
